{
  "scripts": {
    "swarm:init": "docker swarm init",
    "network:create": "docker network create --driver=overlay example_web",
    "network:rm": "docker network rm example_web",
    "volume:create": "docker volume create example_ssb",
    "volume:rm": "docker volume rm example_ssb",
    "stack:deploy": "STACK_NAME=example docker stack deploy --compose-file docker-compose.yml example",
    "stack:ls": "docker stack ls",
    "stack:ps": "docker stack ps example",
    "stack:rm": "docker stack rm example",
    "stack:services": "docker stack services example",
    "sbot": "docker run -it --rm --init -v example_ssb:/home/node/.ssb --net example_ssb -e ssb_host=example_peer-server buttcloud/butt-peer-client"
  }
}
