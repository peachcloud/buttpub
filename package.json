{
  "scripts": {
    "swarm:init": "docker swarm init",
    "network:create": "docker network create --driver=overlay webproxy",
    "network:rm": "docker network rm webproxy",
    "volume:create": "docker volume create example_ssb",
    "volume:rm": "docker volume rm example_ssb",
    "stack:deploy": "docker stack deploy --compose-file docker-compose.yml example",
    "stack:ls": "docker stack ls",
    "stack:ps": "docker stack ps example",
    "stack:rm": "docker stack rm example",
    "stack:services": "docker stack services example",
    "sbot": "docker run -it --rm --init -v example_ssb:/home/node/.ssb --net host buttcloud/butt-peer-client"
  }
}
